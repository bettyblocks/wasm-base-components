package betty-blocks-utilities:pdf-generator@2.0.0;

interface pdf-generator {
    use betty-blocks-utilities:data-api/data-api.{helper-context};

    generate: func(helper-context: helper-context, html: string) -> result<list<u8>, string>;
}

world provider {
    import wasi:http/outgoing-handler@0.2.0;

    export pdf-generator;
}
