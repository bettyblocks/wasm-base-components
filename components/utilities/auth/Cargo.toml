[package]
name = "jwt-auth-component"
edition = "2021"
version = "0.1.0"

[dependencies]
wit-bindgen = "0.47"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
jsonwebtoken = "9.3"
base64 = "0.22"

[dev-dependencies]

[lib]
crate-type = ["cdylib"]
